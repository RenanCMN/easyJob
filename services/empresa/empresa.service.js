app.service('EmpresaService', function($http){
  /*

    crie as requisições para o php aqui
    exemplo:

    this.algumaCoisa = function(parametros){
      return $http({

          method: "POST",
          url : CONFIG.urlLocalHost+'pasta/seuarquivo.php',
          data: parametro, //se nao tiver parametros apague esta linha
          headers: CONFIG.headers

      }).then(function(response) {
        console.log(response); //reponse é um objeto, aperta f12 e veja-o
        return response.data; //data é aonde acessa o retorno do php
      }).catch(function(response){
        return response;
      });
    }

  */
});
